{
  "name": "task-tracker-web",
  "version": "2.0.0",
  "description": "一个简洁的任务追踪器 Web 应用（Node.js + Express + SQLite）",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node test.js && node test-mock-api.js && node test-server.js && node test-offline-store.js && node test-api-offline.js",
    "migrate": "node scripts/migrate.js"
  },
  "private": true,
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
